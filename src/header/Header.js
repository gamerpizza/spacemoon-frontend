import {SearchBar} from "./SearchBar";

function Header() {
    return <header className="Header">
        <p>SPACEMOON</p>
        <SearchBar/>
        <p>User Menu</p>
    </header>;
}

export default Header;
